name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build-apk:
    runs-on: ubuntu-latest
    env:
      VITE_ADMOB_ANDROID_APP_ID: ${{ secrets.VITE_ADMOB_ANDROID_APP_ID }}
      VITE_ADMOB_ANDROID_BANNER_ID: ${{ secrets.VITE_ADMOB_ANDROID_BANNER_ID }}
      VITE_ADMOB_ANDROID_TEST_MODE: ${{ vars.VITE_ADMOB_ANDROID_TEST_MODE }}
      VITE_ADMOB_TEST_DEVICE_IDS: ${{ vars.VITE_ADMOB_TEST_DEVICE_IDS }}
      VITE_ADMOB_DEBUG_GEOGRAPHY: ${{ vars.VITE_ADMOB_DEBUG_GEOGRAPHY }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build web bundle
        run: npm run build

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Sync Capacitor Android
        run: npx cap sync android

      - name: Make Gradle wrapper executable
        run: chmod +x android/gradlew

      - name: Build debug APK
        working-directory: android
        run: ./gradlew assembleDebug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: respirasion-android-debug
          path: android/app/build/outputs/apk/debug/app-debug.apk
